#include <iostream>
#include <algorithm>
#include <set>
using namespace std;

int N, M, ans=0, arr[1005];

int main()
{
    cin >> N >> M;
    for (int i=1; i<=M; i++) {
        int a, b; char c; cin >> a >> b >> c;
        if (c=='T') {

        }
        else {

        }
    }
}
